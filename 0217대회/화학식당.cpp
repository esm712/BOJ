#include<iostream>
#include<algorithm>
#include<string.h>
#include<stack>
using namespace std;

int main(void){
    string s;
    cin >> s;
    int len=s.length();
    cout << len << endl;
    int sum=0;
    int card;
    stack <int> a;
    for(int i=0; i<len; i++){
        if(s[i]='('){
            while(1){
                if()
            }
        }
    }
}